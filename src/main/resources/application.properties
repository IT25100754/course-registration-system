spring.application.name=CourseRegistrationSystem
# =====================================================
# APPLICATION CONFIG
# ====================================================

# =====================================================
# FILE STORAGE PATHS (FIX for your error)
# =====================================================
app.data.students=students.txt
app.data.courses=courses.txt
app.data.registrations=registrations.txt
app.data.payments=payments.txt

# =====================================================
# SERVER CONFIG (optional but useful)
# =====================================================
server.port=8080

# =====================================================
# LOGGING (optional - helps debugging)
# =====================================================
logging.level.org.springframework=INFO
logging.level.com.example=DEBUG

